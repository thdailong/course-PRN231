export const Title = (theme) => ({
  fontWeight: 'bold',
  my: theme.spacing(1.5), // 1 = 8px
  textAlign: 'center',
  color: '#1976D2'
});

export const SubTitle = (theme) => ({
  mb: theme.spacing(1.25),
  textAlign: 'center'
})

export const BottomText = (theme) => ({
  mt: theme.spacing(1.5),
  textAlign: 'center',
});